import { TextField } from '@mui/material';
import React from 'react';

const SearchBar = () =>
	<TextField
		className="searchBar"
		placeholder="Search..."
		size="small"
		onClick={ () => {} }
	>Search
	</TextField>;

export default SearchBar;
